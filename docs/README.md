# Artemis knowledge base

This directory is an Obsidian vault for the Artemis product.

Open `docs/` as a vault in Obsidian, then start at [[Home]]. The vault uses only
standard Markdown and Obsidian core features; no community plugins are required.

